## Simple Refresh Token With Flask
simple refresh token using flask, jwt and sqlalchemy for orm

### For Installation
```sh
$ pip install -r requirements.txt
$ python app.py //for runing the application
```
